name: Update Stream Status

# 運用終了: Cloudflare 版へ移管済み。YouTube API 消費を止めるため無効化。
# 新サイト: https://vhs-city-site-cf.pages.dev/

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired notice
        run: |
          echo "This workflow is retired. Use Cloudflare Worker (vhs-city-site-cf) instead."
          echo "https://vhs-city-site-cf.pages.dev/"
